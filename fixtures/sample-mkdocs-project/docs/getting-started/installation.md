# Installation

This guide covers how to install and set up the project.

## Requirements

- Python 3.8+
- pip package manager

## Steps

1. Create a virtual environment:

   ```bash
   python -m venv venv
   source venv/bin/activate
   ```

2. Install MkDocs Material:

   ```bash
   pip install mkdocs-material
   ```

3. Start the development server:

   ```bash
   mkdocs serve
   ```

!!! tip "Hot Reload"
The development server automatically reloads when you save changes.

## Next Steps

Continue to [Configuration](configuration.md) to customize your site.
