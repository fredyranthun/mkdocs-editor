# Electron-aligned docs

These files are adapted to reflect an Electron + React (TypeScript) implementation (Electron Forge + Vite), replacing prior Tauri/Rust references while keeping the product/architecture intent the same.
